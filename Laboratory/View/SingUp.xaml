<Window x:Class="Laboratory.View.SingUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Laboratory.ViewModel"
        mc:Ignorable="d"
        Title="SingUp" Height="400" Width="800">
    <Window.DataContext>
        <vm:SingUpVM/>
    </Window.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding Collection}" Margin="0,0,0,65"/>
        <Button Content="Save" Command="{Binding SaveCommand}" HorizontalAlignment="Left" Margin="10,354,0,0" VerticalAlignment="Top" Width="75"/>
        <Button Content="Reload" Command="{Binding ReloadCommand}" HorizontalAlignment="Left" Margin="90,354,0,0" VerticalAlignment="Top" Width="75"/>

    </Grid>
</Window>
